## How to Build Images

Run the following commands to build the image:

```bash
# 使用 Singularity
chmod +x make_singularity_image.sh
./make_singularity_image.sh

# 或使用 Apptainer
chmod +x make_apptainer_image.sh
./make_apptainer_image.sh
```
